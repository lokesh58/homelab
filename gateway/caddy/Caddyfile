{
	servers {
		metrics
	}
}

{$DOMAIN}, *.{$DOMAIN} {
	tls {
		dns cloudflare {env.CLOUDFLARE_API_TOKEN}
	}

	log {
		output file /var/log/caddy/access.log {
			roll_size 10mb
			roll_keep 5
		}
		format json
	}

	@vpn host vpn.{$DOMAIN}
	handle @vpn {
		reverse_proxy headscale:8080
	}

	@headplane host vpn.internal.{$DOMAIN}
	handle @headplane {
		reverse_proxy headplane:3000
	}

	handle {
		respond "Not Found" 404
	}
}
