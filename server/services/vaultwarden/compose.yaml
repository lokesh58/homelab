services:
  vaultwarden:
    image: docker.io/vaultwarden/server:1.35.4-alpine
    restart: unless-stopped
    depends_on:
      vaultwarden_db:
        condition: service_healthy
    volumes:
      - vaultwarden_data:/data
    environment:
      DATABASE_URL: postgresql://${VAULTWARDEN_PG_USER}:${VAULTWARDEN_PG_PASS}@vaultwarden_db:5432/${VAULTWARDEN_PG_DB}
      WEBSOCKET_ENABLED: true
      PUSH_ENABLED: true
      PUSH_RELAY_URI: https://api.bitwarden.eu
      PUSH_IDENTITY_URI: https://identity.bitwarden.eu
      PUSH_INSTALLATION_ID: ${VAULTWARDEN_PUSH_INSTALLATION_ID}
      PUSH_INSTALLATION_KEY: ${VAULTWARDEN_PUSH_INSTALLATION_KEY}
      DOMAIN: https://vw.server.${DOMAIN}
      SENDS_ALLOWED: true
      TRASH_AUTO_DELETE_DAYS: 30
      SIGNUPS_ALLOWED: false
      INVITATIONS_ALLOWED: true
      INVITATION_ORG_NAME: Vaultwarden
      ORG_EVENTS_ENABLED: true
      EVENTS_DAYS_RETAIN: 365
      ORG_CREATION_USERS: ${VAULTWARDEN_ORG_CREATION_USERS}
      EXPERIMENTAL_CLIENT_FEATURE_FLAGS: inline-menu-positioning-improvements,inline-menu-totp,ssh-agent,ssh-key-vault-item,pm-25373-windows-biometrics-v2,export-attachments
      ADMIN_TOKEN: ${VAULTWARDEN_ADMIN_TOKEN}
      SMTP_HOST: ${SMTP_HOST}
      SMTP_PORT: ${SMTP_PORT}
      SMTP_USERNAME: ${SMTP_USERNAME}
      SMTP_PASSWORD: ${SMTP_PASSWORD}
      SMTP_FROM: vaultwarden@${DOMAIN}
      SMTP_FROM_NAME: Vaultwarden
      SMTP_SECURITY: ${SMTP_SECURITY}
    networks:
      - vaultwarden_private_net
      - traefik_public_net
    labels:
      - traefik.enable=true
      - traefik.http.routers.vaultwarden.entrypoints=websecure
      - traefik.http.routers.vaultwarden.rule=Host(`vw.server.${DOMAIN}`)
      - traefik.http.services.vaultwarden.loadbalancer.server.port=80

  vaultwarden_db:
    image: docker.io/library/postgres:18.3-alpine3.23
    restart: unless-stopped
    healthcheck:
      interval: 30s
      retries: 5
      start_period: 20s
      test:
        - CMD-SHELL
        - pg_isready -d $${POSTGRES_DB} -U $${POSTGRES_USER}
      timeout: 5s
    environment:
      POSTGRES_DB: ${VAULTWARDEN_PG_DB}
      POSTGRES_USER: ${VAULTWARDEN_PG_USER}
      POSTGRES_PASSWORD: ${VAULTWARDEN_PG_PASS}
    volumes:
      - vaultwarden_db_data:/var/lib/postgresql/data
    networks:
      - vaultwarden_private_net

volumes:
  vaultwarden_data:
  vaultwarden_db_data:

networks:
  vaultwarden_private_net:
    internal: false
